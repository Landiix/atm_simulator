#include <Windows.h>
#include <iostream>
#include <conio.h>
#include <nlohmann/json.hpp>
#include <fstream> 

using namespace std;


static int a()
{
    cout << "Rabotaet";
    /*string *numbers = new string[100];

    fstream CardData;
    CardData.open("CardData.txt");

    int lines = 0;

    while (!CardData.eof())
    {
        lines += 1;
    }

    for (int i = 0;i < lines;i++ )
    while (!CardData.eof())
    {
        getline(CardData,numbers[i]);
    }

    cout << lines << numbers;*/
    return 0;
}

